// arrays are mutable, lists are not (immutable)
val arr = Array(1,2,3)
val arr = Array("a", "b", "C")
val arr = Array(1, 2.2, true)

// making a range
Array.range(0, 10)
Array.range(0, 10, 3)
// can also be called as capital R
Range(0, 14, 2)
